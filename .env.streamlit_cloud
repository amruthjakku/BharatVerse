# Streamlit Cloud Environment Configuration
# These will be set as secrets in Streamlit Cloud

# Database Configuration (will point to your local Docker setup via tunnel)
POSTGRES_HOST=your-tunnel-url.ngrok.io
POSTGRES_PORT=5432
POSTGRES_USER=bharatverse_user
POSTGRES_PASSWORD=secretpassword
POSTGRES_DB=bharatverse

# Redis Configuration
REDIS_HOST=https://lasting-moose-46409.upstash.io
REDIS_PORT=6379

# MinIO Configuration
MINIO_HOST=your-tunnel-url.ngrok.io:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# API Configuration
API_URL=https://your-tunnel-url.ngrok.io:8000

# AI Configuration (lightweight mode for cloud)
AI_MODE=cloud
ENABLE_HEAVY_MODELS=false
USE_LIGHTWEIGHT_MODELS=true

# Security
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# GitLab OAuth (if using)
GITLAB_CLIENT_ID=your-gitlab-client-id
GITLAB_CLIENT_SECRET=your-gitlab-client-secret
GITLAB_REDIRECT_URI=https://your-streamlit-app.streamlit.app/auth/callback